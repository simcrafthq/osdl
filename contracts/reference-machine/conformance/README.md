# Reference machine conformance

This directory contains cases for the reference-machine implementation contract. A runner selects this implementation contract outside an OSDL model document. A model remains compatible without declaring this contract.

This module extends OSDL Core and the DES library. Future contract versions can add cases without changing model documents.
